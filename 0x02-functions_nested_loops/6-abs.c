#include "main.h"
/**
 * _abs - function computes the absolute positive value of an integer
 * @v: parameter to be checked
 * Return: -v or v
 */
int _abs(int v)
{
	if (v < 0)
		return (-v);
	else
		return (v);

}
