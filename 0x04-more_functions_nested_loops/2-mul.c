#include "main.h"
#include <stdio.h>
/**
 * mul - function that multiplies two integers
 * @a: parameter one
 * @b: parameter two
 * Return: always return 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
